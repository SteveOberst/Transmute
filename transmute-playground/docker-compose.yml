version: "3.9"

services:
  playground:
    build:
      context: ..
      dockerfile: transmute-playground/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - TRANSMUTE_STATIC_DIR=/app/static
    restart: unless-stopped
