// transmute-plugins is an umbrella module that groups all official Transmute plugins.
// Each plugin lives in its own sub-directory (e.g. gstreamer/).
// This module does not produce an artifact itself.
