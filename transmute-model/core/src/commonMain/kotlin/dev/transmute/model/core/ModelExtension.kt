package dev.transmute.model.core

/**
 * Extension point for attaching custom data to model types.
 * Downstream consumers can implement this interface to carry
 * format-specific or application-specific information alongside
 * the standard model.
 */
interface ModelExtension
